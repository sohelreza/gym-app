import * as actionTypes from './workoutActionTypes';

const initState = {
    workoutList: []
}

const workoutReducer = (state = initState, action) => {
    switch (actionTypes) {
        default:
            return state
    }
}

export default workoutReducer;